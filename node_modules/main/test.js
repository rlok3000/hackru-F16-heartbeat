var request = require('request');
var jsonObj = {"username":"ok", "password":"no"};
request({
    url: "http://localhost:3000/login",
    method: "POST",
    json: true,   // <--Very important!!!
    body: jsonObj
}, function (error, response, body){
    console.log(body);
});
